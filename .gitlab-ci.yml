image: docker/compose:latest

stages:
  - test

test:
  stage: test
  script:
    - pip install pytest pytest-mock requests flask
    - docker-compose up -d
    - sleep 10
    - pytest tests/test_services/test_service1.py -v
    - docker-compose down
  needs:
    - build